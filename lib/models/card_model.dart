class CardModel {
  final String image, name, jobTitle, id;
  final int starNumber;

  CardModel(
      {required this.image,
      required this.name,
      required this.jobTitle,
      required this.id,
      required this.starNumber});
}
